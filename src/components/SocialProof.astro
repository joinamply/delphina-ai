---
const cases = [
    { title: "FinTech Corp", desc: "Automating compliance checks", img: "https://images.unsplash.com/photo-1551288049-bebda4e38f71?auto=format&fit=crop&w=400&q=80" },
    { title: "HealthAI", desc: "Patient data privacy at scale", img: "https://images.unsplash.com/photo-1576091160399-112ba8d25d1d?auto=format&fit=crop&w=400&q=80" },
    { title: "RetailGiant", desc: "Inventory prediction models", img: "https://images.unsplash.com/photo-1441986300917-64674bd600d8?auto=format&fit=crop&w=400&q=80" }
];
---

<section class="social-proof">
    <div class="container">
        <div class="section-header">
            <h2>Explore use cases & insights</h2>
        </div>

        <div class="case-grid">
            {cases.map(item => (
                <div class="case-card">
                    <img src={item.img} alt={item.title} />
                    <div class="card-content">
                        <h4>{item.title}</h4>
                        <p>{item.desc}</p>
                        <a href="#">Read Story →</a>
                    </div>
                </div>
            ))}
        </div>

        <div class="testimonials-list">
            <h3>What customers say about us</h3>
            <div class="quote-grid">
                <div class="quote-item">
                    <div class="avatar">S</div>
                    <div>
                        <p>"Delphina cut our model deployment time in half."</p>
                        <span>Sarah, CTO</span>
                    </div>
                </div>
                <div class="quote-item">
                    <div class="avatar">M</div>
                    <div>
                        <p>"Finally, data lineage that actually works."</p>
                        <span>Mike, Lead Engineer</span>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<style>
    .social-proof {
        background: var(--color-bg-dark);
        color: white;
        padding: var(--spacing-section) 0;
    }

    .container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 0 var(--spacing-lg);
    }

    .section-header {
        text-align: center;
        margin-bottom: var(--spacing-xl);
    }

    h2 {
        /* Removed Serif */
        font-size: 2.5rem;
        font-weight: 700;
    }

    .case-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: var(--spacing-lg);
        margin-bottom: var(--spacing-section);
    }

    .case-card {
        background: #FDF6E3; /* Cream light */
        color: #1A1A1A;
        border-radius: 8px;
        overflow: hidden;
        transition: transform 0.3s;
    }

    .case-card:hover { transform: translateY(-5px); }

    .case-card img {
        width: 100%;
        height: 200px;
        object-fit: cover;
        filter: grayscale(100%);
        transition: filter 0.3s;
    }

    .case-card:hover img { filter: grayscale(0%); }

    .card-content { padding: 1.5rem; }
    .card-content h4 { font-size: 1.2rem; margin-bottom: 0.5rem; font-weight: 700; }
    .card-content a { color: var(--color-accent); font-weight: 600; display: block; margin-top: 1rem; }

    .testimonials-list {
        text-align: center;
        border-top: 1px solid rgba(255,255,255,0.1);
        padding-top: var(--spacing-xl);
    }
    
    .testimonials-list h3 { margin-bottom: 2rem; color: var(--color-mint); }

    .quote-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: 2rem;
    }

    .quote-item {
        display: flex;
        gap: 1rem;
        align-items: flex-start;
        text-align: left;
    }

    .avatar {
        width: 40px; height: 40px;
        background: #333;
        border-radius: 50%;
        display: flex; align-items: center; justify-content: center;
        font-weight: 700;
    }
</style>
